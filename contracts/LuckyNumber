// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

contract LuckyNumber {
    event Chosen(address indexed who, uint256 number, uint256 ts);

    function pickNumber(uint256 n) external {
        emit Chosen(msg.sender, n, block.timestamp);
    }
}
