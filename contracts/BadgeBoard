// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;
contract BadgeBoard {
    event Badge(address from,address to,string badge);
    function issue(address to,string calldata badge) external { emit Badge(msg.sender,to,badge); }
}
