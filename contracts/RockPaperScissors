// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;
contract RPS {
    event Round(address player,string move,uint ts);
    function play(string calldata move) external { emit Round(msg.sender,move,block.timestamp); }
}
