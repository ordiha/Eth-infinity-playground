// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;
contract FaucetToken { mapping(address=>uint) public bal; uint public drip=100;
  function claim() external { bal[msg.sender]+=drip; }
  function setDrip(uint d) external { drip=d; } // ownerless—open
}
