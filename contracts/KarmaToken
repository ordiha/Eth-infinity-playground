// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;
contract KarmaToken { mapping(address=>uint) public score;
  function give(address to,uint amt) external { score[to]+=amt; }
  function scoreOf(address a) external view returns(uint){ return score[a]; }
}
