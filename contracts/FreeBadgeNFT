// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;
contract FreeBadgeNFT { mapping(uint=>address) public owner; uint public counter;
  function claim() external { counter++; owner[counter]=msg.sender; }
  function ownerOf(uint id) external view returns(address){ return owner[id]; }
}
